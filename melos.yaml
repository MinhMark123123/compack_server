name: compack_server
repository: https://github.com/MinhMark123123/compack_server


packages:
  - compack_server
  # Include all packages inside the `packages` directory that are direct children.
  - packages/*
  # Include all packages inside the `packages` directory and all descendants.
  - packages/**

scripts:
  pub-get:
    exec: dart pub get
    description: Run all static analysis checks.

  int:all:
    run: melos run analyze && melos run format
    description: Run all static analysis checks.

  analyze:
    exec: dart analyze .
  description: Run `flutter analyze` for all packages.

  format:
    run: melos exec dart format . --fix
    description: Run `dart format` for all packages.

  format-check:
    run: melos exec dart format . --set-exit-if-changed
    description: Run `dart format` checks for all packages.

  markdown-check:
    run: markdownlint . --ignore-path .markdownlintignore --config .markdownlint.yaml
    description: Runs the markdown linting check.

  markdown-fix:
    run: markdownlint . --fix --ignore-path .markdownlintignore --config .markdownlint.yaml
    description: Fixes the markdown linting errors.

  dartdoc:
    run: melos exec flutter pub run dartdoc
    description: Run dartdoc checks for all packages.

  test:select:
    run: melos exec -- flutter test
    packageFilters:
      dirExists: test
    description: Run `flutter test` for selected packages.

  test:
    run: melos run test:select --no-select
    description: Run all Flutter tests in this project.

  coverage:
    run: |
      melos exec -- flutter test --coverage &&
      melos exec -- genhtml coverage/lcov.info --output-directory=coverage/
    packageFilters:
      dirExists: test
    description: Generate coverage for the selected package.

  create-dart-package:
    run: sh create_dart_package.sh
    description: Run create package with mason.

  generate:
    run: melos exec -c 1 --depends-on="build_runner" -- "dart pub run build_runner build --delete-conflicting-outputs" #&& melos run generate:flutter
    description: Build all generated files for Dart & Flutter packages in this project

  generate-database:
    run: melos run generate-database:dart #&& melos run generate:flutter
    description: Build all generated files for Dart & Flutter packages in this project

  generate-database:dart:
    run: |
      cd packages/auth_service && pwd && dart pub run build_runner build --delete-conflicting-outputs
      cd ../../
      cd compack_server && pwd && dart pub run build_runner build --delete-conflicting-outputs
    description: Build all generated files for Dart packages in this project.